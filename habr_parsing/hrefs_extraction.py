from dto import HabrArticle


async def extract_habr_hrefs(article: HabrArticle) -> list[str]:
    # TODO
    # Function accepts article and returns all links on page with it (links to habr only)
    return ['URL-1', 'URL-2', 'URL-3', 'URL-4']
